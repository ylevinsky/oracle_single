[project]
name = "local-oracle-rag"
version = "0.1.0"
description = "Local document RAG backed by PostgreSQL/pgvector and Ollama"
requires-python = ">=3.11"
dependencies = [
  "psycopg2-binary>=2.9,<3",
]

[tool.uv]
package = false
