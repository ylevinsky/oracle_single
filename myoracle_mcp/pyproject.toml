[project]
name = "myoracle-mcp"
version = "0.1.0"
description = "Repository-scoped Oracle MCP server with mandatory connection confirmation"
requires-python = ">=3.11"
dependencies = [
  "mcp>=1.27,<2",
  "oracledb>=4,<5",
  "paramiko>=3.4,<4",
  "psycopg[binary]>=3.2,<4",
]

[tool.uv]
package = false
