# Oracle MCP sync process status

2026-08-18. Request: inspect `refresh_mv.py`, add a local Oracle MCP sync-status tool, and update RAG/rules. Scope: Repair Center `TRANSFER_USER` MV sync. Feature: `inspect_saved_sync_process_status(connection_name, history_days=7)` in `oracle_connectivity_mcp/server.py`. It reads `DAILY_TRANSFER_PY` sessions, matching `V$SESSION_LONGOPS`, date-limited `TRANSFER_USER.LOG_PROCEDURE` `DAILY_TRANSFER` records, and `TRANSFER_USER` `DBA_MVIEWS`. `history_days` accepts 1 through 90 and limits audit rows with `PROC_START >= SYSTIMESTAMP - NUMTODSINTERVAL(:history_days, 'DAY')`; it replaces the prior row-count cap. Status rule: active worker `running`; latest `End` `completed`; latest `Error` `failed`; otherwise `unknown`. Verified live result: Repair Center was `completed`; no active workers or long operations; the 2026-08-17 run completed RH at 23:02:48, Hungary at 23:11:36, and Ofakim at 23:30:19. PostgreSQL is running and this record is indexed. The current Codex session may need restart to list the new tool.
